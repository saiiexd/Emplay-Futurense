# API Keys (copy to .env; .env is git-ignored and must never be committed)
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_EXTRACTION_MODEL=gpt-4o

# Provider selection for the extraction engine. Only "openai" is wired today.
LLM_PROVIDER=openai

# Configuration
LOG_LEVEL=INFO
